# --------------- General Settings ---------------

language: java

# required for UI tests
services:
  - xvfb

matrix:
  include:
    # minimum
    # unfortunately JDK 6 is no longer support by Travis CI
    # - dist: trusty
    #   jdk: openjdk6
    # LTS
    - dist: xenial
      jdk: openjdk8
    # LTS
    - dist: xenial
      jdk: openjdk11
    # latest
    - dist: xenial
      jdk: openjdk13

# --------------- Maven Cache ---------------

cache:
  directories:
    - .autoconf
    - $HOME/.m2
