# --------------- General Settings ---------------

dist: xenial
language: java

jdk:
  # unfortunately JDK 6 is no longer support by Travis CI
  # - openjdk6  # minimum
  - openjdk8  # LTS
  - openjdk11 # LTS
  - openjdk13 # latest

# required for UI tests
services:
  - xvfb

# --------------- Maven Cache ---------------

cache:
  directories:
    - .autoconf
    - $HOME/.m2
